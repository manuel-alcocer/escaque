# The public entry point. cloudflared is pointed at this Service from the
# Cloudflare Zero Trust dashboard:
#
#   escaque.linuxarena.net -> http://escaque-frontend.escaque.svc.cluster.local:80
#
# nginx inside the pod serves the built Vue app and proxies /api, /admin and
# /static to the backend, so the browser only ever sees one origin and there is
# no CORS in production.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: escaque-frontend
  namespace: escaque
  labels:
    app.kubernetes.io/name: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: frontend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: frontend
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: frontend
          image: ghcr.io/manuel-alcocer/escaque-frontend:latest
          ports:
            - name: http
              containerPort: 80
          env:
            - name: BACKEND_URL
              value: http://escaque-backend:8000
          resources:
            requests:
              cpu: 20m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 128Mi
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ['ALL']
          # No emptyDir over /var/cache/nginx: the image already ships that
          # directory owned by the nginx user, and mounting over it hands nginx
          # a root-owned mountpoint it then tries to chown — which fails,
          # because dropping ALL capabilities takes CAP_CHOWN with it. The
          # filesystem is not read-only here, so nginx writes to its own paths.
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: frontend
---
apiVersion: v1
kind: Service
metadata:
  name: escaque-frontend
  namespace: escaque
spec:
  selector:
    app.kubernetes.io/name: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
