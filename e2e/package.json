{
  "name": "escaque-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser regression tests. Kept out of frontend/ so they never enter the production image build.",
  "scripts": {
    "test": "node trainer.spec.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
